{
  "scripts": {
    "start": "deno run --import-map=./import_map.json --allow-env --allow-net --allow-read ./mod.ts",
    "dev": "deno run --watch --import-map=./import_map.json --allow-env --allow-net --allow-read ./mod.ts",
    "inspect": "deno run --inspect --import-map=./import_map.json ---allow-env -allow-net --allow-read ./mod.ts"
  }
}
